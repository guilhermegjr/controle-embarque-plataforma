import { FiltroFuncionarioPipe } from './filtro-funcionario.pipe';

describe('FiltroFuncionarioPipe', () => {
	it('create an instance', () => {
		const pipe = new FiltroFuncionarioPipe();
		expect(pipe).toBeTruthy();
	});
});
